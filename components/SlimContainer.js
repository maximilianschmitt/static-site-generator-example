import styled from '@emotion/styled'
import { slimContainerWidth } from '../design'

const SlimContainer = styled.div`
    width: 100%;
    max-width: ${slimContainerWidth};
    margin: 0 auto;
    padding: 0 20px;
`

export default SlimContainer
