import styled from '@emotion/styled'
import { fontSizes, lineHeights } from '../design'

const TextXXL = styled.h2`
    font-size: ${fontSizes.xxl};
    line-height: ${lineHeights.s};
    font-weight: bold;
`

export default TextXXL
